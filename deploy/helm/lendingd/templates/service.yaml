apiVersion: v1
kind: Service
metadata:
  name: {{ include "lendingd.fullname" . }}
  labels:
    {{- include "lendingd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: grpc
  selector:
    app.kubernetes.io/name: {{ include "lendingd.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
